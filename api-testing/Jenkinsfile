pipeline {
    agent any

    stages {
        stage('Compile') {
            steps {
               
               bat 'mvn clean compile'
               
            }
        }
        stage('Testing') {
            steps {
                
                    bat 'mvn clean test'
                
            }
        }
      }
}
